# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## English / 简体中文 / 繁體中文

### Project Status / 项目状态 / 專案狀態

**AI-value** is a newly initialized repository in early planning phase. No code, build system, or development tools have been configured yet.

**AI-value** 是一个新初始化的代码库，处于早期规划阶段。尚未配置代码、构建系统或开发工具。

**AI-value** 是一個新初始化的代碼庫，處於早期規劃階段。尚未配置代碼、構建系統或開發工具。

### Repository Information / 仓库信息 / 倉庫信息

- **License:** Apache 2.0
- **Remote:** https://github.com/kmjade/AI-value.git
- **Primary branch:** `main`
- **IDE:** IntelliJ IDEA (`.idea/` directory present)

- **许可证：** Apache 2.0
- **远程仓库：** https://github.com/kmjade/AI-value.git
- **主分支：** `main`
- **IDE：** IntelliJ IDEA（存在 `.idea/` 目录）

- **許可證：** Apache 2.0
- **遠程倉庫：** https://github.com/kmjade/AI-value.git
- **主分支：** `main`
- **IDE：** IntelliJ IDEA（存在 `.idea/` 目錄）

### Current State / 当前状态 / 當前狀態

The repository contains minimal files:
- `README.md` - Project title only
- `LICENSE` - Apache 2.0
- `.gitignore` - Ignores `.idea/` directory
- `.claude/` - Empty directory for Claude Code configuration

仓库包含最少的文件：
- `README.md` - 仅有项目标题
- `LICENSE` - Apache 2.0
- `.gitignore` - 忽略 `.idea/` 目录
- `.claude/` - Claude Code 配置的空目录

倉庫包含最少的文件：
- `README.md` - 僅有專案標題
- `LICENSE` - Apache 2.0
- `.gitignore` - 忽略 `.idea/` 目錄
- `.claude/` - Claude Code 配置的空目錄

### Development Notes / 开发说明 / 開發說明

The project's purpose, technology stack, and architecture have not yet been determined. Initial `.gitignore` entries suggested LangChain/LangGraph but were removed, indicating the tech stack is undecided.

项目的目的、技术栈和架构尚未确定。最初的 `.gitignore` 条目建议使用 LangChain/LangGraph，但已被移除，表明技术栈尚未决定。

專案的目的、技術棧和架構尚未確定。最初的 `.gitignore` 條目建議使用 LangChain/LangGraph，但已被移除，表明技術棧尚未決定。

When implementing features:
1. Define the project's purpose and goals first
2. Choose appropriate technology stack
3. Set up build system, testing, and linting tools
4. Create proper documentation

实现功能时：
1. 首先定义项目的目的和目标
2. 选择合适的技术栈
3. 设置构建系统、测试和代码检查工具
4. 创建适当的文档

實現功能時：
1. 首先定義專案的目的和目標
2. 選擇合適的技術棧
3. 設置構建系統、測試和代碼檢查工具
4. 創建適當的文檔
